import { Task } from '@src';
export type MerkleRedeemDeployment = {
    Vault: string;
    rewardToken: string;
};
declare const _default: {
    arbitrum: {
        Vault: Task;
        rewardToken: string;
    };
};
export default _default;
